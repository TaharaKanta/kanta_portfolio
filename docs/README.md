# docs index

`docs` 配下のドキュメント一覧です。

## 運用ルール

- `docs/` 配下を更新したコミットでは、pre-commit フックで `npm run refactor` を自動実行する。

- プロジェクト仕様:
  `docs/project-spec.md`
- フロントエンド設計方針:
  `docs/frontend-architecture.md`
- フォルダ構成ガイド:
  `docs/folder-structure.md`
- ファイル作成ルール:
  `docs/file-creation-rules.md`
